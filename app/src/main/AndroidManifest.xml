<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI" />

    <application
        android:name=".app"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="n">
        <!-- Activities -->
        <!-- INTRO -->
        <!--TJ-->


        <!-- SplashScreen -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- GetStartedScreenActivity -->
        <activity android:name=".GetStartedScreenActivity"/>
        <!-- OurOffersScreenActivity -->
        <activity android:name=".OurOffersScreenActivity"/>

        <!-- Tj -->
        <!-- WelcomeMessageScreen -->
        <activity android:name=".WelcomeMessageScreenActivity"/>
        <!-- BriefAboutScreen -->
        <activity android:name=".BriefAboutScreenActivity"/>
        <!-- TJHomeScreen -->
        <activity android:name=".TJHomeScreenActivity"/>
        <!-- ProductCategoryScreenActivity-->
        <activity android:name=".ProductCategoryScreenActivity"/>
        <!-- ProductDetailsScreen -->
        <activity
            android:name=".ProductDetailsScreenActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <!-- ProductsScreen -->
        <activity
            android:name=".ProductsScreenActivity"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <!-- AboutScreen -->
        <activity android:name=".AboutCompanyScreenActivity1"/>





        <!-- WelcomeScreen -->


        <!-- LogRegScreen -->
        <activity android:name=".LogRegScreenActivity" />

        <!-- LoginScreen -->
        <activity android:name=".LoginScreenActivity" />

        <!-- OtpCodeScreen -->
        <activity android:name=".OtpCodeScreenActivity" />

        <!-- RegistrationScreen -->
        <activity android:name=".RegistrationScreenActivity" />
        <!-- HOME -->
        <!-- HomeScreen-->
        <activity
            android:name=".HomeScreenActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppThemeDashboard" />

        <!-- CRBInfoScreen -->
        <activity android:name=".CRBInfoScreenActivity" />

        <!-- CRBProvidersScreen -->
        <activity android:name=".CRBProvidersScreenActivity" />
        <activity android:name=".CRBProvider1Activity" />
        <activity android:name=".CRBProvider2Activity" />
        <activity android:name=".CRBProvider3Activity" />
        <!-- ProductsScreen-already declared as part of INTRO -->

        <!-- InquiriesFormScreen -->
        <activity android:name=".InquiriesFormScreenActivity" />
        <!-- AboutCompanyScreen -->
        <activity
            android:name=".AboutCompanyScreenActivity"
            android:theme="@style/AppThemeDashboard" />
        <!-- LoanStatusScreen -->
        <activity android:name=".LoanStatusScreenActivity" />

        <!-- RateScreen -->
        <activity android:name=".RateScreenActivity" />

        <!-- LOAN APPLICATION -->
        <!-- TermsAndConditionsScreen-->
        <activity android:name=".TermsAndConditionsScreenActivity" />

        <!-- FirstTimeUserScreen -->
        <activity android:name=".FirstTimeUserScreenActivity" />

        <!-- PhoneDetailsScreen -->
        <activity android:name=".PhoneDetailsScreenActivity" />

        <!-- UserDetailsScreen -->
        <activity android:name=".UserDetailsScreenActivity" />

        <!-- EducationalLevelScreen -->
        <activity android:name=".EducationalLevelScreenActivity" />

        <!-- ApplicationPurposeScreen -->
        <activity android:name=".ApplicationPurposeScreenActivity" />

        <!-- IncomeDetailsScreen -->
        <activity android:name=".IncomeDetailsScreenActivity" />

        <!-- IncomeDetailsScreen1 -->
        <activity android:name=".IncomeDetailsScreen1Activity" />

        <!-- IdentificationDetailsScreen -->
        <activity android:name=".IdentificationDetailsScreenActivity" />

        <!-- LoanPurposeScreen -->
        <activity android:name=".LoanPurposeScreenActivity" />

        <!-- LoanAmountScreen -->
        <activity android:name=".LoanAmountScreenActivity" />

        <!-- CommitmentScreen -->
        <activity android:name=".CommitmentScreenActivity" />

        <!-- MetaData -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>
        <meta-data android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token"/>



    </application>

</manifest>